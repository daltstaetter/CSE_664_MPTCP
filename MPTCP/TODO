TODO 04/08/2019:

	-Test/Confirm if MPTCP is actually creating additional subflows
	-Attempt to capture MPTCP_(CAPABLE|JOIN) flags using TCP dump 
	 when pinging amiusingmptcp.de's IP address using my VM (cmd line,
	 uses tcpdump, any other tools?)
	-Create Socket Application to read/ping amiusingmptcp.de and 
	 confirm if it is using >1 MPTCP sublflow
    -Find if there is a way to print/broadcast the IP addresses that
     have been connected to and to/from which data has been sent

